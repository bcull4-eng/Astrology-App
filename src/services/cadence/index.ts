/**
 * Update Cadence Resolver
 *
 * Determines when dashboard elements should be refreshed
 * based on defined cadence rules from the PRD.
 */

export type {
  CadenceConfig,
  CadenceRule,
  UpdateCadenceService,
} from './types'

export { DEFAULT_CADENCE_CONFIG } from './types'

// Implementation will be added here
// export { createUpdateCadenceService } from './service'
